# Deep Search Pro 操作手册

> **适用场景**：重启电脑后，从零把项目跑起来，并在前端页面完成交互测试。
> **预计耗时**：启动约 30 秒，一轮完整测试约 15 分钟。
> **最重要的一句话**：MySQL 没有注册成 Windows 服务，**重启后不会自己启动**，必须手动拉起（见第 1 步）。
>
> **路径约定**：本文档用 `<项目根目录>` 表示项目所在的文件夹、`<MYSQL_HOME>` 表示
> MySQL 的安装目录（本机可能是 `D:\MySQL`）。请按你自己的实际路径替换后再执行。

---

## 一、开始测试：三步走

### 第 1 步 · 一键启动

**双击 `<项目根目录>\start_all.bat`**

脚本会依次做三件事，期间会弹出两个黑窗口：

1. 弹出 **「MySQL 8.4 - Deep Search Pro」** 窗口 —— 启动数据库
2. 等约 12 秒，弹出 **「Deep Search API - Deep Search Pro」** 窗口 —— 启动 Web 服务
3. 再等约 10 秒，自动打开浏览器 `http://localhost:8000/`

> **两个黑窗口全程保持开着**。停止服务请双击 `stop_all.bat`，**不要直接关窗口**（原因见第八节）。

### 第 2 步 · 确认启动成功（三个检查点）

| # | 看哪里 | 正常表现 |
|---|---|---|
| 1 | MySQL 窗口 | 最后几行出现 `ready for connections` 和 `port: 3306` |
| 2 | API 窗口 | 出现 `Uvicorn running on http://0.0.0.0:8000` |
| 3 | 浏览器页面 | 左上显示 **🤖 Deep Search Pro**，右栏写着「暂无进度，先发一个任务吧」 |

三项都符合 → 环境 OK，直接进第 3 步。

浏览器若提示"无法访问此网站"，通常是 API 还没起好：**等 10 秒按 F5** 即可。

### 第 3 步 · 发第一个任务，验证链路

在左下输入框粘贴下面这句，按 **Ctrl + Enter**（或点「发送」）：

```
查询数据库中销售额最高的 5 种药品，给出名称、分类和销售额并分析
```

**判断成功的标志（三处会同时联动）**：

| 位置 | 现象 |
|---|---|
| 右上角状态灯 | 灰点「未连接」→ **绿点「实时通道已连接」** |
| 右栏「执行进度」 | 开始逐条冒出新记录，带 🔧 / 🤝 / ✅ 图标和时间戳 |
| 左侧「对话」 | 出现智能体给出的完整回答（含具体的药品名和金额） |

**这三处都出现，说明整条链路是通的**，可以继续往下测。

---

## 二、页面怎么读

界面就四块：

| 区域 | 位置 | 看什么 |
|---|---|---|
| 对话 | 左栏上半 | 你的提问 + 智能体最终回复 |
| 输入框 | 左栏底部 | 输入任务，**Ctrl + Enter** 发送；执行中「发送」按钮会临时禁用 |
| 执行进度（实时推送） | 右栏上半 | **核心区域** —— 智能体每一步动作 + 时间戳 |
| 生成的文件 | 右栏下半 | 产出文件：文件名 + 大小 + 「下载」链接 |

**时间线图标 = 智能体的动作语言**，认图标就知道它在干嘛：

| 图标 | 含义 |
|---|---|
| 🔧 | 调用了工具（联网搜索 / SQL 查询 / 读写文件） |
| 🤝 | 调度子智能体、模型思考中 |
| ✅ | 任务完成（结果同时出现在左侧对话） |
| ❌ | 出错 |
| ℹ️ | 会话创建等状态信息 |

---

## 三、交互测试用例清单

按顺序问，每条都写了预期结果：

| # | 场景 | 输入这句 | 预期 |
|---|---|---|---|
| 1 | 数据库查询 | 查询数据库中销售额最高的 5 种药品，给出名称、分类和销售额并分析 | 时间线出现 🔧数据库工具 + 🤝；回答里有真实药品名和金额 |
| 2 | 联网搜索 | 搜索 2026 年医药电商行业的主要趋势，总结三点 | 出现 🔧搜索工具；回答带来源信息 |
| 3 | 生成文件 | 把刚才的结论整理成一份 Markdown 报告，文件名 pharma_report.md | 右栏「生成的文件」出现该条目；点「下载」能拿到文件 |
| 4 | 转 PDF | 把刚才的报告转成 PDF | 文件列表多出一个 `.pdf` |
| 5 | 多轮追问 | 那第二名是哪个？ | 正确接上文（同一会话，智能体记得上下文） |
| 6 | 混合调度 | 对比一下数据库里的销售情况和网上的行业趋势 | 时间线出现**多个** 🤝（依次调度数据库 + 搜索两个子智能体） |
| 7 | 知识库 | 布洛芬和对乙酰氨基酚有什么区别？ | ⚠️ 会失败并降级 —— RAGFlow 未接通，**已知限制，不是 bug** |
| 8 | 错误路径 | 先用 `<MYSQL_HOME>\stop_mysql.bat` 停掉 MySQL（或直接关窗口），再问"数据库里有多少种药品" | 出现 ❌ 报错但服务不崩；重新启动 MySQL 后换个问法可恢复 |

> 用例 1-6 是"正常功能验收"，7-8 是"边界与容错验收"。8 条全过，前端功能就算验完了。

---

## 四、三条必须知道的交互规则

不搞清楚这三条，很容易把正常现象误判成 bug：

1. **多轮追问共用同一个会话** —— 页面用同一个 `thread_id` 连续提问，所以智能体记得上文（问"那第二名呢"能接上）。**要测独立场景必须先按 F5 刷新页面开新会话**，否则上一轮的结论会串进来干扰
2. **每次点「发送」会清空右栏**（时间线和文件列表都清空），左侧对话历史保留。想看上一轮的文件，趁下次发送前看
3. **发第一个任务之前，右上角状态灯显示「未连接」是正常的** —— 页面此时还没建立 WebSocket 连接，发出任务后才会变绿

---

## 五、进阶：用 F12 看原始数据

想确认"推送到底有没有发出来"，按 `F12`：

- **Network 标签** → 筛选框输入 `ws` → 点开 `ws/<thread_id>` → **Messages** 标签
  能看到每条原始 JSON 报文，形如：
  ```json
  {"type":"monitor_event","event":"tool_start","message":"..."}
  ```
  右侧时间线就是这些报文的可视化。任务卡住时看这里最准。
- 切到 **Fetch/XHR** 筛选：
  - `/api/task` 的响应里有 `thread_id`（**这个名字就是本次会话的产物目录名**）
  - `/api/files` 的响应是文件清单

---

## 六、文件产物在哪

```
<项目根目录>\output\session_<thread_id>\
```

每个会话一个独立文件夹，`thread_id` 就是第五节里拿到的那个名字。页面「下载」按钮取的就是这个目录下的文件。

---

## 七、常见问题排查

| 现象 | 原因 / 处理 |
|---|---|
| 双击 `start_all.bat` 后浏览器打不开页面 | 等 10 秒按 F5；还不行就用附录 A 手动启动，能看到具体报错 |
| API 窗口报 `address already in use` / 端口被占 | 上次的服务没退干净。开 CMD 跑 `netstat -ano \| findstr :8000`，记下最后一列 PID，再 `taskkill /F /PID <该PID>` |
| MySQL 窗口一闪就没了 | 大概率 3306 被占（可能已有一个 MySQL 在跑）。跑 `netstat -ano \| findstr :3306` 看有没有 LISTENING |
| 状态灯一直灰色「未连接」 | 还没发任务，正常。发个任务就会变绿；发过任务仍是灰的，看 API 窗口有没有报错 |
| 回答里出现"内容审核未通过 / Content Exists Risk" | DeepSeek 服务端内容风控拦截，非项目问题。换中性表述重问，或**按 F5 开新会话**（老会话带着被拦内容会反复失败） |
| 问知识库问题总是失败 | RAGFlow 未接通（云端 API 需付费档、自建需 16GB 内存机器），项目按设计降级，其他子智能体不受影响 |
| 数据库问题报错 | 检查 MySQL 窗口是否还开着、里面有没有报错 |
| 页面能开但任务一直没结果 | 智能体多轮搜索可能要 1-3 分钟，耐心等；超过 5 分钟看 API 窗口日志 |

---

## 八、测试结束后的收尾

1. **停止服务：双击 `<项目根目录>\stop_all.bat`**
   - 它依次停掉 API 服务（8000 端口）和 MySQL（3306 端口，走 `mysqladmin shutdown` 优雅关闭，10 秒未停才降级强杀）
   - 末尾会打印两行检查结果，都显示 `FREE` 才算真的停了
   - ⚠️ **不要再靠"直接关闭两个黑窗口"来停服务**：`start_mysql.bat` 用 `mysqld --console` 前台启动，关窗口走的是 Windows 控制台关闭事件，只给进程几秒钟，MySQL 的 fast shutdown 常常跑不完，会在 `<MYSQL_HOME>\data` 留下脏 PID 文件和未清理的临时表空间（`ibtmp1`），下次启动要做 InnoDB 恢复。而且 `--console` 会覆盖 `my.ini` 里的 `log-error`，**错误日志根本不落盘**，关掉窗口后现场无从追查
   - 只想停其中一个：API 在运行窗口按 `Ctrl+C`；MySQL 双击 `<MYSQL_HOME>\stop_mysql.bat`（会提示输入 root 密码）
   - 想确认到底停没停：`netstat -ano | findstr ":3306 :8000"` —— **没有任何输出 = 全停了**；窗口开着不代表服务在跑，窗口关了也不代表服务已停，只有端口是可信的
   - 想彻底告别手动启停：右键 `<MYSQL_HOME>\install_service_admin.bat` → 以管理员身份运行，注册成 `MySQL84` 服务后可用 `net stop MySQL84` 停止，且错误日志会正常写入文件
   - **停完之后浏览器里"页面还在"，别急着怀疑没停干净** —— 服务端是否已停只看端口（上一条）。静态页现在已带 `Cache-Control: no-store`（`api/server.py` 里的 `NoCacheStaticFiles`），从根上关掉了浏览器的"启发式缓存"；但本次改动**之前**访问过的页面可能仍留在本地缓存，所以改后第一次请按 `Ctrl + Shift + R` 硬刷新（或开无痕窗口）清掉旧的，之后按 F5 就会如实反映真实状态
   - 想当场验证"服务确实已停"：在原页面发一条消息（WebSocket 连不上），或在地址栏重新访问（会报 `ERR_CONNECTION_REFUSED`）
2. 产出文件在 `<项目根目录>\output\` 下，想重跑一轮干净测试可以把旧会话目录删掉（不影响任何配置）
3. 会话记录落在 `<项目根目录>\data\checkpoints.db`，需要"抹掉记忆"就删这个文件（下次启动会自动重建）

---

## 附录 A · 手动启动（一键脚本不好使时）

**① 启动 MySQL** —— 双击 `<MYSQL_HOME>\start_mysql.bat`，窗口保持开着。

**② 启动 API 服务** —— 双击 `<项目根目录>\start_api.bat`，窗口保持开着。

> 如果脚本报"找不到带 `uvicorn` 的解释器"，说明这台机器上的 Python 环境不止一个（例如同时装了 Anaconda）。
> `start_api.bat` 会自己按 `DSP_PYTHON` → `.venv` → `.dsp-python.txt` → PATH 的顺序逐个实测挑选，
> 四个都不可用时才报错。依赖装在项目外时，把解释器路径写进项目根的 `.dsp-python.txt`（一行即可）。
> 详见 `TESTING.md` 第 0 节的「解释器怎么定」。

两步都完成后，浏览器打开 `http://localhost:8000/`。

**想让它开机自启、以后不用管**：
右键 `<MYSQL_HOME>\install_service_admin.bat` → **以管理员身份运行**，把 MySQL 注册成 Windows 服务（名称 `MySQL84`）。之后 MySQL 会随开机自动启动，就不用每次双击了。（API 服务仍需手动启动。）

---

## 附录 B · 关键路径速查

| 用途 | 路径 |
|---|---|
| 一键启动 | `<项目根目录>\start_all.bat` |
| **一键停止** | `<项目根目录>\stop_all.bat` |
| 只启动 API | `<项目根目录>\start_api.bat` |
| 启动 / 停止 MySQL | `<MYSQL_HOME>\start_mysql.bat` / `<MYSQL_HOME>\stop_mysql.bat` |
| 注册 MySQL 服务（管理员） | `<MYSQL_HOME>\install_service_admin.bat` |
| 配置文件（密钥、数据库连接） | `<项目根目录>\.env` |
| 文件产物 | `<项目根目录>\output\session_<thread_id>\` |
| 会话记录 | `<项目根目录>\data\checkpoints.db` |
| 详细测试手册（含单元测试、接口测试） | `<项目根目录>\TESTING.md` |

---

## 附录 C · 不需要重新配置的东西

重启电脑**不会丢**，也**不用重填**：

- LLM 与 Tavily 的 API Key（在 `.env` 里）
- 数据库数据（40 种药品 + 240 条销售记录，在 `<MYSQL_HOME>\data`）
- Python 依赖环境（已装好，含 pandas / weasyprint / mysql-connector 等）
- VS Code 调试与任务配置（`.vscode/`）
- 前端页面、提示词、子智能体代码

**唯一每次都要做的**：双击 `start_all.bat` 把两个服务拉起来。
